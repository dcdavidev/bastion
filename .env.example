# Server Configuration
PORT=8080

# Database Configuration
DATABASE_URL=postgres://user:password@localhost:5432/bastion?sslmode=disable

# Admin Authentication (Argon2id)
# Use a tool or a small script to generate these
ADMIN_PASSWORD_HASH=
ADMIN_PASSWORD_SALT=

# Key Wrapping
# 32-byte hex encoded master key used to wrap/unwrap data keys
MASTER_KEY=
