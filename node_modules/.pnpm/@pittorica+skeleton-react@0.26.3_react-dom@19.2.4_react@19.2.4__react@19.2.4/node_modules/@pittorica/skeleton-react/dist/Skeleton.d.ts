import { type ElementType } from 'react';
import { type BoxProps } from '@pittorica/box-react';
export type SkeletonProps<E extends ElementType = 'div'> = BoxProps<E> & {
    /**
     * If true, shows the skeleton. If false, shows the children.
     * @default true
     */
    loading?: boolean;
    /**
     * Shape of the skeleton.
     * @default 'rect'
     */
    variant?: 'rect' | 'circle' | 'text';
};
/**
 * Skeleton placeholder for loading states.
 * Fully polymorphic and agnostic foundation.
 */
export declare const Skeleton: {
    <E extends ElementType = "div">({ children, loading, variant, className, style, as, ...props }: SkeletonProps<E>): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
//# sourceMappingURL=Skeleton.d.ts.map