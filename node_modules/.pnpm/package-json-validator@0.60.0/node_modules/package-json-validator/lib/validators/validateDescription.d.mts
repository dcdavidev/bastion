import { Result } from "../Result.mjs";

//#region src/validators/validateDescription.d.ts

/**
 * Validate the `description` field in a package.json.
 * It should be a non-empty string..
 */
declare const validateDescription: (value: unknown) => Result;
//#endregion
export { validateDescription };