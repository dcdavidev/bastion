import { Result } from "../Result.mjs";

//#region src/validators/validateKeywords.d.ts

/**
 * Validate the `keywords` field in a package.json, which should be an array of
 * strings.
 *
 * ["eslint", "package.json"]
 */
declare const validateKeywords: (obj: unknown) => Result;
//#endregion
export { validateKeywords };