import { Result } from "../Result.mjs";

//#region src/validators/validateLicense.d.ts

/**
 * Validate the `license` field in a package.json, using `validate-npm-package-license`.
 */
declare const validateLicense: (license: unknown) => Result;
//#endregion
export { validateLicense };