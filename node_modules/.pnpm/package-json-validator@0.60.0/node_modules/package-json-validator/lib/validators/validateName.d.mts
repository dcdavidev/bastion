import { Result } from "../Result.mjs";

//#region src/validators/validateName.d.ts

/**
 * Validate the `name` field in a package.json, using `validate-npm-package-name`.
 */
declare const validateName: (value: unknown) => Result;
//#endregion
export { validateName };