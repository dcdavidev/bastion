import { Result } from "../Result.mjs";

//#region src/validators/validateHomepage.d.ts

/**
 * Validate the `homepage` field in a package.json.
 * It should be a string that contains a url.
 */
declare const validateHomepage: (value: unknown) => Result;
//#endregion
export { validateHomepage };