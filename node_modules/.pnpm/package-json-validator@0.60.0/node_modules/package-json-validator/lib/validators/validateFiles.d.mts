import { Result } from "../Result.mjs";

//#region src/validators/validateFiles.d.ts

/**
 * Validate the `files` field in a package.json, which should be an array of
 * file patterns.
 *
 * ["dist", "CHANGELOG.md"]
 * https://docs.npmjs.com/cli/v11/configuring-npm/package-json#files
 */
declare const validateFiles: (obj: unknown) => Result;
//#endregion
export { validateFiles };