import { Result } from "../Result.mjs";

//#region src/validators/validateCpu.d.ts

/**
 * Validate the `cpu` field in a package.json, which should be an array of
 * strings.
 *
 * ["x64", "ia32"]
 */
declare const validateCpu: (obj: unknown) => Result;
//#endregion
export { validateCpu };