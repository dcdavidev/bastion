import { Result } from "../Result.mjs";

//#region src/validators/validateWorkspaces.d.ts

/**
 * Validate the `workspaces` field in a package.json, which should be an array of
 * file patterns.
 *
 * ["./app", "./packages/*"]
 * @see https://docs.npmjs.com/cli/v11/configuring-npm/package-json#workspaces
 */
declare const validateWorkspaces: (value: unknown) => Result;
//#endregion
export { validateWorkspaces };