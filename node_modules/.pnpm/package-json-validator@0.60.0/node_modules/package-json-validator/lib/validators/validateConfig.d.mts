import { Result } from "../Result.mjs";

//#region src/validators/validateConfig.d.ts

/**
 * Validate the `config` field in a package.json. The value
 * should be an object.
 */
declare const validateConfig: (obj: unknown) => Result;
//#endregion
export { validateConfig };