import { Result } from "../Result.mjs";

//#region src/validators/validateMain.d.ts

/**
 * Validate the `main` field in a package.json.
 * It should be a non-empty string.
 */
declare const validateMain: (value: unknown) => Result;
//#endregion
export { validateMain };