import { UrlType } from "./validation.types.mjs";

//#region src/utils/validateUrlTypes.d.ts

/**
 * Format for license(s) and repository(s):
 * url as a string
 * or
 * object with "type" and "url"
 * or
 * array of objects with "type" and "url"
 */
declare const validateUrlTypes: (name: string, obj: string | UrlType | UrlType[]) => string[];
//#endregion
export { validateUrlTypes };