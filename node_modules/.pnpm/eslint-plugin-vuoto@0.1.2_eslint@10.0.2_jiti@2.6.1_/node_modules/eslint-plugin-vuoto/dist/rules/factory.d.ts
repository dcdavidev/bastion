import type { Rule } from 'eslint';
/**
 * Create a rule that disallows a set of characters.
 * @param regex The regex pattern to match.
 * @param description The rule description.
 * @param replacement The string to replace the match with.
 * @param messageId The message ID.
 * @returns The rule module.
 */
export declare function createWhitespaceRule(regex: RegExp, description: string, replacement?: string, messageId?: string): Rule.RuleModule;
//# sourceMappingURL=factory.d.ts.map