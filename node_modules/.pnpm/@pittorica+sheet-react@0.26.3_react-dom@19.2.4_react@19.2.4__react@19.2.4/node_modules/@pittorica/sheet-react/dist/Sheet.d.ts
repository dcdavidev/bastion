import { type ElementType, type ReactNode } from 'react';
import { type BoxProps } from '@pittorica/box-react';
export type SheetSide = 'top' | 'right' | 'bottom' | 'left';
/**
 * Fix TS2314: Use 'type' and generic E for polymorphic BoxProps.
 */
export type SheetProps<E extends ElementType = 'div'> = BoxProps<E> & {
    isOpen: boolean;
    onClose: () => void;
    children: ReactNode;
    /**
     * Direction from which the sheet appears.
     * @default 'right'
     */
    side?: SheetSide;
    title?: string;
    appearance?: 'light' | 'dark' | 'inherit';
};
/**
 * Sheet component following MD3 guidelines for modal panels.
 * Fully polymorphic and agnostic foundation.
 */
export declare const Sheet: {
    <E extends ElementType = "div">({ isOpen, onClose, children, side, title, appearance, className, as, ...props }: SheetProps<E>): import("react/jsx-runtime").JSX.Element | null;
    displayName: string;
};
//# sourceMappingURL=Sheet.d.ts.map